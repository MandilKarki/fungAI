"""Optional protocol integrations — each has its own optional dependency
(see pyproject.toml extras) and is lazy-imported, so importing
aegis_core.integrations itself never requires mcp/acp to be installed."""
